// 外層迴圈：控制被乘數 i
for (let i = 1; i <= 9; i++) {
    let row = ""; // 每一排開始前，先準備一個空字串來收集這排的結果
    
    // 內層迴圈：控制乘數 j
    for (let j = 1; j <= 9; j++) {
        // 將計算結果組合成字串，並加上縮排符號 \t
        row += `${i}x${j}=${i*j}\t`;
    }
    
    // 內層跑完 9 次後，將這一整排印出
    console.log(row);
}